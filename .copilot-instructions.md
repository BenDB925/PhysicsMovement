# Copilot Agent Instructions - PhysicsDrivenMovementDemo

> Agent-facing workspace guide. Optimised for GPT-5.4 style task routing, not humans.

---

## Project Summary

Multiplayer ragdoll brawler (Gang Beasts-style). 2-4 players control physics-driven active-ragdoll characters that grab, punch, throw, and knock each other out. Built in **Unity 6 (6000.3.9f1, Built-in 3D RP)** with **Netcode for GameObjects (NGO)** for peer-to-peer networking. Characters are *always* ragdolls - muscle forces (ConfigurableJoint SLERP drives) keep them upright; zeroing those forces *is* the ragdoll/knockout state. Physics is host-authoritative at 100 Hz.

---

## Mandatory Reading - Load These First

| File | What it contains |
|------|-----------------|
| `CODING_STANDARDS.md` | **All** workflow, style, testing, architecture, documentation, and pre-commit review rules. Follow the Phase A→F workflow for every task. |
| `ARCHITECTURE.md` | High-level system diagram, data flow, key classes per system, integration points. |
| `TASK_ROUTING.md` | Fast task-to-file map. Read this after the standards/architecture pass when the task is new and you need to know where to start. |
| `PLAN.md` | Short index for the unified locomotion roadmap. If the task is roadmap-driven locomotion work, follow it into `Plans/unified-locomotion-roadmap.plan.md`; use `.github/instructions/unified-locomotion-roadmap.instructions.md` only as the chapter-routing shim. |
| `Plans/README.md` | Task record tree rules. Read when the user provides a task list, asks for a plan, or the work is likely to need child work-package docs or bug sheets. |
| `AGENT_TEST_RUNNING.md` | How to run focused EditMode/PlayMode test slices or wider suites from the terminal, parse NUnit XML results, and troubleshoot failures. |

---

## Context Budget Rules

- When a doc exposes `Quick Load` or `Read More When`, read those sections first and stop unless the task clearly needs deeper detail.
- Prefer the active parent plan plus the freshest artifact summary before opening raw XML, long logs, or older child docs.
- Do not read `LOCOMOTION_BASELINES.md` end to end unless the task is about regression comparison, known reds, or baseline verification.
- Do not read full unified locomotion roadmap chapters unless the task matches that chapter's scope; use `PLAN.md`, `TASK_ROUTING.md`, and the roadmap routing table to pick only the relevant chapter.
- For test work, read `TestResults/latest-summary.md` first when it exists, then the relevant NUnit XML under `TestResults/`, and only then the full Unity log if the summary or XML is insufficient.

---

## Systems & Namespaces

| Namespace / Folder | Purpose | Key Classes |
|--------------------|---------|-------------|
| `Character/` | Ragdoll body, balance, movement, state machine, camera, and debug/demo helpers | `RagdollSetup`, `BalanceController`, `GroundSensor`, `PlayerMovement`, `LegAnimator`, `ArmAnimator`, `CharacterState`, `LocomotionCollapseDetector`, `CameraFollow`, `FallPoseRecorder`, `DebugPushForce`, `LapDemoRunner`, `RagdollMeshFollower` |
| `Character/Locomotion/` | Internal locomotion contracts plus the shared Chapter 2 sensor aggregation, observation filtering, Chapter 3 leg-state labels, per-leg state machines, and director-owned support/leg command flow | `LocomotionDirector`, `SupportObservationFilter`, `LocomotionSensorAggregator`, `LocomotionSensorSnapshot`, `SupportGeometry`, `DesiredInput`, `FootContactObservation`, `SupportObservation`, `LocomotionObservation`, `BodySupportCommand`, `LegCommandOutput`, `LegStateMachine`, `LegStateType`, `LegStateTransitionReason`, `LegStateFrame`, `LocomotionLeg`, `LegCommandMode` |
| `Core/` | Game-wide config, utilities, constants | `GameSettings` |
| `Input/` | New Input System action maps | `PlayerInputActions` |
| `Environment/` | Runtime room metadata for generated arenas | `ArenaRoom` |
| `Editor/` | Editor-only build tools (not shipped) | `RagdollBuilder`, `SceneBuilder`, `ArenaBuilder`, `PropBuilder`, `SkinnedRagdollBuilder` |
| `Networking/` | Reserved for Phase 5 work | *(folder exists, no runtime code yet)* |
| `UI/` | Reserved for Phase 6 work | *(folder exists, no runtime code yet)* |
| `GameLoop/` | Reserved for Phase 6 work | *(folder exists, no runtime code yet)* |
| `Tests/EditMode/` | NUnit EditMode tests (pure logic) | `GameSettingsTests`, `PlayerRagdollPrefabTests`, `ArmAnimatorTests`, `LocomotionContractsTests`, `LocomotionDirectorEditModeTests` |
| `Tests/PlayMode/` | NUnit PlayMode tests (scene/physics/integration) | `BalanceControllerTests`, `PlayerMovementTests`, `LegAnimatorTests`, `LocomotionDirectorTests`, `GaitOutcomeTests`, `HardSnapRecoveryTests`, `TurnRecoveryTests` |

> **Update this table whenever a new system, namespace, or key class is added.**

### Primary Scenes

| Scene | Purpose |
|------|---------|
| `Assets/Scenes/Arena_01.unity` | Main physics movement validation scene used by the current prototype and many movement investigations. |
| `Assets/Scenes/Museum_01.unity` | Generated museum concept scene built around `ArenaBuilder` and `ArenaRoom`. |

---

## Architecture Snapshot

```
Input (Update)                Network (ServerRpc)
     │                              │
     ▼                              ▼
 ┌────────────────────────────────────────┐
 │              HOST PHYSICS             │
 │   PlayerMovement (forces + intent)    │
 │   LocomotionDirector (command owner)  │
 │   BalanceController (support executor) │
 │   CharacterState (FSM)                │
 │   LegAnimator (leg executor)          │
 │   ArmAnimator (counter-swing)         │
 │   FallPoseRecorder (NDJSON)           │
 │   HandGrabber (FixedJoints)           │
 │   HitReceiver (knockout)              │
 │           FixedUpdate @ 100 Hz        │
 └────────────────┬───────────────────────┘
        │
     NetworkRigidbody sync
        │
     ┌────────────┴────────────┐
     ▼                         ▼
  Client 2                  Client 3 …
 (kinematic)               (kinematic)
```

- **Clients** read input and send via `ServerRpc`. They do NOT apply physics forces (unless local prediction is added later).
- **Host** runs PhysX, applies all forces, owns all Rigidbody authority.
- **NetworkRigidbody** replicates transform state to clients. Core bodies (hips, torso) sync at 30 Hz; extremities at 15 Hz.

---

## Key Constraints & Decisions

| Constraint | Detail |
|------------|--------|
| Always-ragdoll | No kinematic mode. "Standing" = joint drives active. "Knocked out" = joint drives zeroed. |
| Host-authoritative physics | Avoids desync. Clients are visual-only for physics objects. |
| Primitives first | Characters are capsules/boxes until physics feel is right. Art comes last (Phase 7). |
| 100 Hz physics | `Time.fixedDeltaTime = 0.01f`. Required for joint stability with 15-body ragdolls. |
| PD balance (no I term) | Integral term causes windup with ragdolls. Use Proportional + Derivative only. |
| Layer-based self-collision filtering | Each player has a dedicated physics layer. `RagdollSetup` also calls `Physics.IgnoreCollision` for neighbouring parts. |

---

## Project-Specific Rules (Extend CODING_STANDARDS.md)

1. **All physics tuning parameters** (`kP`, `kD`, joint springs/dampers, force magnitudes, speed caps) must be `[SerializeField]` with `[Range]` attributes - never hardcoded literals.
2. **Joint configuration data** (angle limits, drive springs, mass) should live in ScriptableObjects or serialized structs so ragdoll variants are data-driven.
3. **Do not set `Rigidbody.velocity` directly** for gameplay movement. Use `AddForce` / `AddTorque` with appropriate `ForceMode`. Velocity clamping via `ClampMagnitude` is acceptable as a safety cap only.
4. **State machine transitions** must be explicit and logged at `Debug.Log` level during development (guarded by a `[SerializeField] bool _debugStateTransitions`).
5. **Networking guard pattern** - every `NetworkBehaviour` method that applies gameplay logic must be guarded:
   - Input reading: `if (!IsOwner) return;`
   - Physics / state mutation: `if (!IsServer) return;`
   - Visual-only client effects: `if (IsServer) return;`
6. **Task record tree** - When the user provides a task list, asks for a plan, or the work will span multiple meaningful steps, create or update a task record tree. Use the user-specified folder if given; otherwise default to `Plans/` and follow `Plans/README.md`. The parent plan stays short and is the canonical entry point.
7. **Hard rollover threshold** - Split a parent section into a child work-package doc once it would exceed 80 lines, needs more than 3 dated progress notes, or requires raw logs or telemetry. Create a linked bug sheet once an investigation would exceed 120 lines, more than 30 lines of raw logs or telemetry, or more than one active bug or hypothesis thread.
8. **Completion review hook** - Workspace hooks under `.github/hooks/` watch for user prompts that signal task completion and block the first stop so the agent reviews task records and documentation before ending the session.

---

## Phase Status Tracker

Update this section as phases are completed so future agents know the current state.

| Phase | Status | Notes |
|-------|--------|-------|
| 0 - Project Setup | **Complete** | Unity 6000.3.9f1, packages added, physics configured, layers 8-12 set, folder structure created. |
| 1 - Ragdoll Skeleton | **Complete** | `RagdollSetup.cs` done. `RagdollBuilder` Editor tool creates prefab. Tests passing. |
| 2 - Balance Controller | **Complete** | 2A joint drives, 2B `GroundSensor.cs`, 2C `BalanceController.cs` - PD torque, IsFallen, SetFacingDirection. All tests passing. |
| 3A - Player Input | **Complete** | Input action wiring done. |
| 3B1-3B3 - PlayerMovement | **Complete** | Movement forces, speed cap, `SetMoveInputForTest` seam added. |
| 3C1-3C3 - FSM + Get-Up | **Complete** | `CharacterState.cs` scaffold, transitions, fallen timer, get-up impulse + recovery timeout. |
| 3C4 - Locomotion Collapse Detection | **Complete (2026-03-08)** | `LocomotionCollapseDetector.cs` adds a bounded root-level fall signal for strong-intent/no-progress/rear-support collapse cases. `CharacterState` consumes it to enter `Fallen`; later roadmap cleanup keeps the detector on the watchdog path instead of letting executors read it directly. |
| 3D1 - Yaw Torque Split | **Complete (2026-02-22)** | Split upright (pitch/roll) and yaw (world Y) into separate PD torques. `_kPYaw`, `_kDYaw` added. Airborne multiplier upright-only. |
| 3D2 - Yaw Stability Hardening | **Complete (2026-02-22)** | Dead zone `_yawDeadZoneDeg` (default 2°), normalised yaw vectors, zero-input facing retention, NaN guards. 59/59 PlayMode + 9 EditMode passing. |
| **3E1 - LegAnimator Core** | **Complete (2026-02-22)** | `LegAnimator.cs` on Hips. Phase accumulator from move input magnitude. Sinusoidal upper-leg alternation (L/R offset π). Constant knee-bend lower legs. Fallen/GettingUp bypass to identity. 18 new PlayMode tests; 77/77 PlayMode + 9/9 EditMode passing. |
| **3E2 - Leg Settle & Idle Blend** | **Complete (2026-02-22)** | `_idleBlendSpeed` serialized field added. Phase decay toward 0 on idle. `_smoothedInputMag` ramps 0→1 on gait re-entry (anti-pop). Legs snap to identity when idle. 8 new PlayMode tests; 85/85 PlayMode passing. |
| **3E fixes** | **Complete (2026-02-22)** | Fixed joint axis (Z→X via `_swingAxis`), bumped leg springs to 1200 Nm/rad, enforced Slerp drive mode. Knee-lift regression test added (`LowerLeg_WhenWalking_LiftsBeyondMinimumThreshold`, 5° threshold, outcome-based). 101/101 passing. |
| **3E3 - World-Space Leg Swing** | **Complete (2026-02-22)** | `_useWorldSpaceSwing` path added. Leg swing can be computed relative to world-space movement direction (velocity proxy, XZ input fallback). `ApplyWorldSpaceJointTarget` converts world rotation to connected-body local frame. Knee bend uses the same world-horizontal axis so knees never fold into ground regardless of torso pitch. Current code still initializes `_useWorldSpaceSwing` to `false`, so confirm the intended default before changing behaviour. |
| **3E4 - ArmAnimator** | **Complete (2026-02-23)** | `ArmAnimator.cs` added - counter-swings arms opposite to legs using `LegAnimator.Phase` and `LegAnimator.SmoothedInputMag` (two new public properties). UpperArm SLERP drives added to `RagdollSetup` (spring=800, damper=80, maxForce=3000). LowerArm/Hand joints remain floppy. 3 new EditMode tests; 12/12 EditMode passing. |
| 3F1 - Jump Impulse | **Complete (2026-02-23)** | `_jumpForce = 15f` [SerializeField] added to `PlayerMovement`. `SetJumpInputForTest(bool)` seam added (matches `SetMoveInputForTest` pattern). Jump gated on Standing/Moving state + `IsGrounded`. `AddForce(Vector3.up * _jumpForce, ForceMode.Impulse)` on Hips Rb. One-frame consume via `_jumpPressedThisFrame` flag (cleared unconditionally in `TryApplyJump()`). Space key wired via existing `PlayerInputActions.Player.Jump` binding. 5 new PlayMode tests; 112/128 total (15 pre-existing LegAnimator failures from tuned defaults, not introduced by 3F1). |
| 3F2 - Airborne Leg Spring | **Complete (2026-02-23)** | Springs scale to 0.15× airborne, restore on landing. `CaptureBaselineDrives()` moved to `Start()`. Guard added for zero-baseline edge case. `GaitOutcomeTests.cs` added (4 outcome tests). 129/131 passing. |
| 3G1 - Camera Follow | **Complete (2026-02-23)** | `CameraFollow.cs` delivered by Jermie — orbital third-person, mouse/stick orbit, pitch clamp, SphereCast collision avoidance, cursor lock. |
| 3G2 - Camera Scene Wiring | **Complete (2026-02-23)** | `CameraFollow` wired to Main Camera in Arena_01. Fixed camera-relative movement freeze at ±180° (yaw error clamped, yaw gate removed from LegAnimator). |
| 3T - Phase 3 Test Pass | **Complete (2026-02-23)** | Full PlayMode + EditMode suite passing. New test files: `ArmAnimatorPlayModeTests`, `CameraFollowTests`, `SpinRecoveryTests`, `GetUpReliabilityTests` (3 [Ignore] — synthetic rig can't measure hips tilt), `FullStackSanityTests`. All optimizer tests marked [Ignore] for CI (run on-demand only). 165 total tests, 146 passing, 14 skipped ([Ignore]), 5 [Ignore] with known root causes documented. |
| Roadmap C1.1 - Baseline Lock | **Complete (2026-03-12)** | Captured pre-director outcome metrics from `GaitOutcomeTests`, `HardSnapRecoveryTests`, `SpinRecoveryTests`, `MovementQualityTests`. Baseline snapshot saved in `LOCOMOTION_BASELINES.md`. Known pre-existing reds: `WalkStraight_NoFalls`, `SustainedLocomotionCollapse_TransitionsIntoFallen`. |
| Roadmap C1.2 - Internal Locomotion Contracts | **Complete (2026-03-12)** | Added internal contract types under `Assets/Scripts/Character/Locomotion/` plus reflection-backed EditMode coverage in `Assets/Tests/EditMode/Character/LocomotionContractsTests.cs`. No runtime rewiring yet. |
| Roadmap C1.3 - LocomotionDirector Skeleton | **Complete (2026-03-12)** | Added `LocomotionDirector` on the PlayerRagdoll Hips in pass-through mode. It snapshots `DesiredInput` from `PlayerMovement`, aggregates live locomotion observations, and mirrors legacy support plus gait commands without changing current executors. Focused verification passed via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` and `LocomotionDirectorTests` + `PlayerMovementTests` + `FullStackSanityTests`. |
| Roadmap C1.4 - Ownership Boundaries Rewired | **Complete (2026-03-12)** | `PlayerMovement` now reports desired locomotion and jump intent without directly retargeting `BalanceController`. `LocomotionDirector` now owns support recovery detection and publishes `BodySupportCommand` plus explicit leg command frames into `BalanceController` and `LegAnimator`. Focused EditMode seam coverage passed 6/6, and the targeted broader Chapter 1 PlayMode slice passed 107/110 with 3 ignored and 0 failures. |
| Roadmap C1.5 - Safety Role Cleanup | **Complete (2026-03-12)** | `LocomotionCollapseDetector` now remains a watchdog signal into `CharacterState` and director observations only; `PlayerMovement`, `BalanceController`, and pass-through leg command generation no longer gate directly on the raw detector. Focused PlayMode boundary coverage passed 46/46 via `CharacterStateTests` + `PlayerMovementTests` + `BalanceControllerTurningTests` + `LocomotionDirectorTests`, and the Chapter 1 EditMode seam slice stayed green 6/6 via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests`. |
| Roadmap C1.6 - Regression Gate | **Complete (2026-03-12)** | EditMode 18/18 green. Chapter 1 PlayMode gate 111/114 (3 ignored, 0 failures). Baseline outcome slice 10/12 with the two pre-existing `MovementQualityTests` reds unchanged. Full suite test-order sensitivity remains for `SpinRecoveryTests` and `HardSnapRecoveryTests` (pass in isolation). Details in `LOCOMOTION_BASELINES.md`. |
| Roadmap C2.1 - Observation Schema | **Complete (2026-03-12)** | Added internal `FootContactObservation` and `SupportObservation` contracts plus support quality, contact confidence, planted-foot confidence, slip estimate, turn severity, and COM-outside-support fields on `LocomotionObservation`. `LocomotionDirector` now seeds baseline support/contact/turn signals from the current foot grounded state and desired-vs-body heading without changing pass-through execution; COM-outside-support remains conservative until Chapter 2.2 adds explicit support geometry aggregation. Focused verification passed via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` (`9/9`) and `LocomotionDirectorTests` (`3/3`). |
| Roadmap C2.2 - Sensor Aggregation | **Complete (2026-03-12)** | Added shared `SupportGeometry`, `LocomotionSensorSnapshot`, and `LocomotionSensorAggregator` helpers so `LocomotionDirector` and `LocomotionCollapseDetector` now consume the same per-step foot-contact, hips-velocity, yaw-rate, and support-geometry sample. `GroundSensor` now preserves its last ground point, `LocomotionObservation.IsComOutsideSupport` is driven by the aggregated support capsule, and focused verification passed 10/10 in EditMode plus 16/16 for `LocomotionDirectorTests` + `BalanceControllerIntegrationTests` in PlayMode. The broader slice that included `MovementQualityTests` still only showed the two pre-existing reds tracked in `LOCOMOTION_BASELINES.md`. |
| Roadmap C2.3 - Confidence And Hysteresis | **Complete (2026-03-12)** | Added `SupportObservationFilter` plus serialized C2.3 thresholds on `LocomotionDirector` so per-foot contact and planted confidence now use temporal filtering and hysteresis before entering `LocomotionObservation`. `FootContactObservation` now exposes a stable `IsPlanted` flag, and the prefab-backed runtime slice verifies that a one-frame foot slide no longer flips the planted classification immediately. Focused verification passed 13/13 in EditMode via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` and 17/17 in PlayMode via `LocomotionDirectorTests` + `BalanceControllerIntegrationTests`. |
| Roadmap C2.4 - Debug Visibility | **Complete (2026-03-12)** | `LocomotionDirector` now computes a per-step predicted drift direction and cached observation telemetry line, with optional serialized `Debug.DrawLine` support-geometry visualisation and throttled console logging for support quality, contact confidence, planted confidence, slip, turn severity, and planted-foot state. Focused verification passed 3/3 in EditMode via `LocomotionDirectorEditModeTests` and 18/18 in PlayMode via `LocomotionDirectorTests` + `BalanceControllerIntegrationTests`. |
| Roadmap C2.5 - Director Observation Decisions | **Complete (2026-03-12)** | `LocomotionDirector` now converts Chapter 2 observations into body-support recovery plus upright/yaw/stabilization strength scales instead of relying on raw previous-direction history. Focused verification passed 15/15 in EditMode via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` and 20/20 in PlayMode via `LocomotionDirectorTests` + `BalanceControllerIntegrationTests`. A broader gate (`MovementQualityTests` + `HardSnapRecoveryTests` + `StumbleStutterRegressionTests`) only held the two pre-existing isolated `MovementQualityTests` reds after a rerun confirmed the extra mixed-slice `TurnAndWalk_CornerRecovery` failure was slice-order-sensitive. |
| Roadmap C3.1 - Leg State Model | **Complete (2026-03-12)** | Added internal `LegStateType`, `LegStateTransitionReason`, and `LegStateFrame` contracts under `Assets/Scripts/Character/Locomotion/`, and threaded explicit state + reason labels through `LegCommandOutput`. `LegAnimator` now classifies pass-through gait frames into `Stance` / `Swing` / `Plant` roles, promotes `RecoveryStep` and `CatchStep` labels from weak-support recovery conditions, and logs per-leg state transitions through `_debugStateTransitions` without changing the current gait execution path. Focused verification passed 16/16 in EditMode via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` and 9/9 in PlayMode via `LocomotionDirectorTests`; the broader feature slice `LegAnimatorTests` + `GaitOutcomeTests` + `StumbleStutterRegressionTests` passed with 63 passing and 3 ignored. |
| Roadmap C3.2 - Per-Leg Controller | **Complete (2026-03-12)** | Added internal `LegStateMachine` so left and right legs now own independent phase, state, and transition-reason progression instead of always deriving the right leg from `left + π`. `LegAnimator` still exposes the legacy left-phase seam and sinusoidal executor path, but swing/catch-step roles now guarantee a meaningful forward arc, behind-body catch-step promotion only fires for materially trailing feet or confirmed collapse, and the director-facing command frame now preserves per-leg controller state at runtime. Focused verification passed 19/19 in EditMode via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests`, and the broader Chapter 3 PlayMode slice `LocomotionDirectorTests` + `LegAnimatorTests` + `GaitOutcomeTests` + `StumbleStutterRegressionTests` passed with 73 passing, 3 ignored, and 0 failed. |
| Roadmap C3.3 - Animator Bridge | **In progress (2026-03-12)** | `LegAnimator.ApplyCommandFrame()` now resolves explicit `Swing`, `Stance`, `Plant`, and `CatchStep` roles into per-leg upper-leg and knee targets before applying the existing joint executor, and both local-space and world-space joint application paths now accept independent left/right knee bends. Focused verification passed 19/19 in EditMode via `LocomotionContractsTests` + `LocomotionDirectorEditModeTests` and 57/60 in the dedicated `LegAnimatorTests` slice (3 ignored, 0 failed). The broader mixed PlayMode slice `LocomotionDirectorTests` + `LegAnimatorTests` + `GaitOutcomeTests` + `StumbleStutterRegressionTests` still leaves `LowerLeg_WhenWalking_FeetAlternate` unstable/failing under some run shapes, so keep this work package open until that gait-quality regression is resolved. |
| 4 - Grabbing & Combat | Not started | |
| 5 - Networking | Not started | |
| 6 - Game Loop & UI | Not started | |
| 7 - Polish & Juice | Not started | |

---

## Quick Workflow Reminder

Every task follows **CODING_STANDARDS.md §1** - the six mandatory phases:

```
A. Understand → B. Comment Design → C. Write Tests (red) → D. Implement (green) → E. Verify → F. Self-Review (F1-F10 checklist)
```

Present the F1-F10 self-review table to the user before committing. See `CODING_STANDARDS.md` §1 Phase F for the full checklist.

If the task is plan-driven or will span multiple meaningful steps, create or update the task record tree early and keep the parent plan current as child docs or bug sheets change.

When the task is unfamiliar, open `TASK_ROUTING.md` before broad searching. It reflects the current implemented surface, including reserved-but-empty folders.

Running tests yourself can take a long time and fail often. Present the user with the option to run tests themselves. If they ask you to run them, follow this guide:

   -Run tests from the terminal using the commands in `AGENT_TEST_RUNNING.md`. Do not ask the user to open the Test Runner window.

   -Default to focused verification for the touched feature: run the directly affected suites plus nearby regression/outcome tests for that runtime loop. Only run the full EditMode + PlayMode suite when the change is cross-cutting or the impact cannot be bounded confidently.


### Unity Test Lock Guardrail

- Never run two Unity batch commands in parallel for the same project path.
- Before running tests, ensure no Unity process is open for this project.
- Run EditMode first, then PlayMode, and verify that `TestResults/*.xml` timestamps are freshly updated.
- Prefer the absolute-path `-NoProfile` invocation of `Tools/Run-UnityTests.ps1` for better terminal reliability.
- If Unity exits but `TestResults/*.xml` is missing, treat it as transient infra/lock race and rerun (up to 2-3 attempts) before concluding failure.

### Test Quality Rule - Outcomes over Inputs

**Prefer outcome-based tests over input-based tests.**

- ❌ **Weak:** Assert that `targetRotation` was set to a certain value → only proves the input was applied
- ✅ **Strong:** Assert that the joint/transform *physically moved* to a measurable position or angle → proves the system actually worked

Examples of outcome-based tests:
- A leg lift test that asserts `LowerLeg.position.y` exceeds a minimum threshold after N physics frames of walking - not just that `targetRotation` was set
- A balance test that asserts the character's hips stay within X degrees of upright after N frames - not just that torque was applied
- A get-up test that asserts the character reaches a standing hip height - not just that `GettingUp` state was entered

**Why:** Input-based tests can pass even when the physics system is completely broken (wrong axis, weak springs, wrong drive mode). Outcome-based tests catch real regressions. When in doubt, ask: *"would this test have caught the dragging-feet bug?"* If no, make it stronger.

Every new PlayMode test should have at least one outcome assertion alongside any input/state assertions.

---

*Update this file whenever systems, namespaces, architecture, constraints, or phase status change.*
